# Emmon Coral 2026

This directory contains small scripts used to annotate various formats of input data (e.g. different file types, different information),
with various annotations.
These scripts may be used directly for annotating data where appropriate, but are more likely to be used as a basis to create new
scripts handling slightly different inputs.
